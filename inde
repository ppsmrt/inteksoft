<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
<title>Official Admin Dashboard</title>
<script src="https://cdn.tailwindcss.com"></script>
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet"/>
<style>
  body {
    font-family: 'Inter', sans-serif;
    background: #f9fafb;
    color: #111827;
    min-height: 100vh;
  }

  /* Glass cards */
  .glass-card {
    backdrop-filter: blur(8px);
    background: #ffffff;
    border-radius: 1rem;
    border: 1px solid #e5e7eb;
    transition: all 0.3s ease;
    cursor: pointer;
    animation: fadeUp 0.5s ease forwards;
  }
  .glass-card:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 10px 25px rgba(0,0,0,0.1);
  }
  .glass-card i {
    transition: transform 0.3s ease;
  }
  .glass-card:hover i {
    transform: rotate(-15deg) scale(1.2);
  }

  /* Fullscreen iframe overlay */
  #iframeOverlay {
    position: fixed;
    top:0; left:0; width:100%; height:100%;
    background: rgba(255,255,255,0.95);
    z-index:50; display:none; flex-direction:column;
    animation: fadeIn 0.3s ease forwards;
  }
  #iframeOverlay iframe { flex:1; border:none; width:100%; }
  #iframeOverlay .closeBtn {
    background: #f3f4f6; border-bottom:1px solid #e5e7eb;
    padding:1rem; text-align:right; font-weight:600; cursor:pointer; transition: background 0.2s;
  }
  #iframeOverlay .closeBtn:hover { background: #e5e7eb; }

  /* Bottom nav */
  .nav-btn.active span { color: #2563eb; font-weight:600; }
  .gradient-icon { background: linear-gradient(45deg,#2563eb,#3b82f6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; transition: all 0.3s; }
  .nav-btn.active .gradient-icon { background: linear-gradient(45deg,#1d4ed8,#2563eb); }

  /* Floating Action Button */
  .fab { position: fixed; bottom: 20px; right: 20px; width: 60px; height: 60px; border-radius: 50%; 
         background: linear-gradient(45deg,#2563eb,#3b82f6); color:white; display:flex; justify-content:center; align-items:center; 
         box-shadow:0 5px 15px rgba(0,0,0,0.2); cursor:pointer; transition: all 0.3s; z-index:30; }
  .fab:hover { transform: scale(1.1); }

  @keyframes fadeIn { from{opacity:0} to{opacity:1} }
  @keyframes fadeUp { from{opacity:0;transform:translateY(20px);} to{opacity:1;transform:translateY(0);} }
</style>
</head>
<body class="flex flex-col">

<!-- Header -->
<header class="w-full bg-white border-b border-gray-200 shadow-md fixed top-0 left-0 z-20">
  <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
    <div class="flex items-center gap-2 font-bold text-xl">
      <i class="fas fa-layer-group text-blue-600"></i>
      <span>Inteksoft Dashboard</span>
    </div>
  </div>
</header>

<!-- Main -->
<main class="flex-1 flex flex-col items-center justify-start px-4 pt-28 pb-10 w-full max-w-6xl mx-auto">
  <div class="text-center mb-6">
    <h1 class="text-4xl font-extrabold">🌐 Admin Portal</h1>
    <p class="mt-1 text-lg text-gray-600">Access Inteksoft tools & links in one place</p>
  </div>

  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 w-full">
    <div onclick="openFullscreen('https://tnm3u.live/admin')" class="glass-card p-5 flex justify-between items-center">
      <div class="flex items-center gap-3">
        <i class="fas fa-tv text-blue-500 text-2xl"></i>
        <span class="font-semibold text-lg">Add TV Channel</span>
      </div>
      <i class="fas fa-arrow-up-right-from-square text-gray-400"></i>
    </div>
    <div onclick="openFullscreen('https://tnm3u.live/approve')" class="glass-card p-5 flex justify-between items-center">
      <div class="flex items-center gap-3">
        <i class="fas fa-play text-red-500 text-2xl"></i>
        <span class="font-semibold text-lg">Approve TV Channel</span>
      </div>
      <i class="fas fa-arrow-up-right-from-square text-gray-400"></i>
    </div>
    <div onclick="openFullscreen('https://tnm3u.live/fm/admin')" class="glass-card p-5 flex justify-between items-center">
      <div class="flex items-center gap-3">
        <i class="fas fa-radio text-orange-600 text-2xl"></i>
        <span class="font-semibold text-lg">Add Radio Channel</span>
      </div>
      <i class="fas fa-arrow-up-right-from-square text-gray-400"></i>
    </div>
    <div onclick="openFullscreen('https://www.tnm3u.live/upload')" class="glass-card p-5 flex justify-between items-center">
      <div class="flex items-center gap-3">
        <i class="fas fa-upload text-green-600 text-2xl"></i>
        <span class="font-semibold text-lg">Upload Image</span>
      </div>
      <i class="fas fa-arrow-up-right-from-square text-gray-400"></i>
    </div>
    <div onclick="openFullscreen('https://ppsmrt.github.io/adora/admin')" class="glass-card p-5 flex justify-between items-center">
      <div class="flex items-center gap-3">
        <i class="fas fa-user text-red-600 text-2xl"></i>
        <span class="font-semibold text-lg">Tenant Directory</span>
      </div>
      <i class="fas fa-arrow-up-right-from-square text-gray-400"></i>
    </div>
  </div>
</main>

<!-- Fullscreen Iframe -->
<div id="iframeOverlay" class="flex">
  <div class="closeBtn" onclick="closeFullscreen()"><i class="fas fa-arrow-left mr-2"></i> Back</div>
  <iframe id="fullscreenFrame" src=""></iframe>
</div>

<!-- Floating Action Button -->
<div class="fab" onclick="openFullscreen('https://tnm3u.live/admin')">
  <i class="fas fa-plus text-2xl"></i>
</div>

<!-- Bottom Navigation -->
<nav class="fixed bottom-0 left-0 w-full bg-white border-t border-gray-200 shadow-lg z-20">
  <div class="max-w-6xl mx-auto flex justify-around py-3">
    <button onclick="setActiveNav(this); openFullscreen('https://tnm3u.live/admin')" class="flex flex-col items-center text-gray-500 nav-btn active">
      <i class="fas fa-layer-group text-xl gradient-icon"></i>
      <span class="text-xs mt-1">Dashboard</span>
    </button>
    <button onclick="setActiveNav(this); openFullscreen('https://tnm3u.live/approve')" class="flex flex-col items-center text-gray-500 nav-btn">
      <i class="fas fa-tv text-xl gradient-icon"></i>
      <span class="text-xs mt-1">TV</span>
    </button>
    <button onclick="setActiveNav(this); openFullscreen('https://tnm3u.live/fm/admin')" class="flex flex-col items-center text-gray-500 nav-btn">
      <i class="fas fa-radio text-xl gradient-icon"></i>
      <span class="text-xs mt-1">Radio</span>
    </button>
    <button onclick="setActiveNav(this); openFullscreen('https://www.tnm3u.live/upload')" class="flex flex-col items-center text-gray-500 nav-btn">
      <i class="fas fa-upload text-xl gradient-icon"></i>
      <span class="text-xs mt-1">Upload</span>
    </button>
    <button onclick="setActiveNav(this); openFullscreen('https://ppsmrt.github.io/adora/admin')" class="flex flex-col items-center text-gray-500 nav-btn">
      <i class="fas fa-user text-xl gradient-icon"></i>
      <span class="text-xs mt-1">Tenant</span>
    </button>
  </div>
</nav>

<!-- Footer -->
<footer class="border-t border-gray-200 mt-auto w-full">
  <div class="max-w-6xl mx-auto px-6 py-4 flex flex-col sm:flex-row items-center justify-between text-sm text-gray-500">
    <p>© 2025 Inteksoft. All rights reserved.</p>
    <p class="flex items-center gap-1">
      <i class="fas fa-shield-alt text-green-600"></i>
      Secured & Trusted
    </p>
  </div>
</footer>

<script>
const overlay = document.getElementById('iframeOverlay');
const frame = document.getElementById('fullscreenFrame');
const navButtons = document.querySelectorAll('.nav-btn');

function openFullscreen(url){
  frame.src = url;
  overlay.style.display = 'flex';
  history.pushState({iframeOpen:true}, "Iframe Open");
}

function closeFullscreen(){
  frame.src='';
  overlay.style.display='none';
  if(history.state && history.state.iframeOpen) history.back();
}

// Swipe down to close
let startY=0;
overlay.addEventListener('touchstart', e=>startY=e.touches[0].clientY);
overlay.addEventListener('touchmove', e=>{
  let moveY = e.touches[0].clientY-startY;
  if(moveY>100) closeFullscreen();
});

// Back button handling
window.addEventListener('popstate', ()=>{ if(overlay.style.display==='flex') closeFullscreen(); });

// Set active nav
function setActiveNav(button){
  navButtons.forEach(btn=>btn.classList.remove('active'));
  button.classList.add('active');
}
</script>
</body>
</html>
